{
  "name": "fees-computation-cli",
  "version": "1.0.0",
  "engines": {
    "bun": ">=1.0.0"
  },
  "description": "Compute fees for a specific vault within a block range",
  "main": "index.js",
  "scripts": {
    "compute": "bun index.ts compute",
    "find-blocks": "bun index.ts find-blocks",
    "find-claimable-controllers": "bun index.ts find-claimable-controllers",
    "generate": "bunx --bun graphql-codegen --config graphql-codegen-cli/fetch-schema.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bun": "^1.2.4",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^13.1.0",
    "@graphql-codegen/cli": "^5.0.5",
    "commander": "^13.1.0",
    "env-var": "^7.5.0",
    "graphql": "^16.10.0",
    "graphql-request": "^7.1.2",
    "viem": "^2.23.2"
  }
}
